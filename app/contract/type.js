module.exports = {
  // 默认接口类型
  config: {
    nickName: { type: 'string', required: true, example: '' },
    password: { type: 'string', required: true, example: '' },
    phoneNum: { type: 'string', required: true, example: '' },
    email: { type: 'string', required: true, example: '' },
  },
}
